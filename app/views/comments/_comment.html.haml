%p
  %strong Commenter:
  = comment.commenter
%p
  %strong Comment:
  = comment.body
/- if current_user.email.eql?("admin@email.admin")
%p
  =link_to 'Delete comment', [comment.hotel, comment], method: :delete, data: {confirm: 'Are you sure?'}, class: 'button'
%hr
