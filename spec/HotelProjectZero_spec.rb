require 'spec_helper'

describe HotelProjectZero do
  it 'has a version number' do
    expect(HotelProjectZero::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
